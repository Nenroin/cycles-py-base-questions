### Часть 1: Основы и цикл `while`

**Блок 1: Общие понятия**

1. Что такое цикл в программировании и для чего он нужен? Приведи пример из реальной жизни (не связанный с программированием).  
2. Какие два основных типа циклов есть в Python?  
3. Что такое итерация? (Объясни своими словами).  
4. Что такое тело цикла?  
5. Что такое условие выхода из цикла и почему оно важно?  

**Блок 2: Цикл `while` (цикл с условием)**

6. Как записывается базовая структура цикла `while`?  
7. Как работает цикл `while`? (Опиши словами порядок его выполнения).  
8. Что произойдет, если условие цикла `while` изначально ложно? Выполнится ли тело цикла хоть раз?  
9. Что такое бесконечный цикл и как его создать?  
10. Почему бесконечные циклы — это плохо? Как их обычно останавливают при случайном создании?  
11. Какая команда позволяет немедленно выйти из цикла, не дожидаясь проверки условия?  
12. Какая команда позволяет пропустить оставшуюся часть текущей итерации и сразу перейти к следующей?  
13. Напиши код, который с помощью `while` выводит числа от 1 до 5.  
14. Напиши код, который с помощью `while` просит пользователя вводить число, пока он не введет 0.  

### Часть 2: Цикл `for`

**Блок 3: Цикл `for` и функция `range()`**  

15. Как записывается базовая структура цикла `for`?  
16. Чем цикл `for` принципиально отличается от `while`? (`while` работает пока `True`, а `for` — ...?).  
17. Что такое итерируемый объект? Назови 3 примера таких объектов в Python.  
18. Для чего нужна функция `range()`? Опиши, что делают три ее формы:  
    *   `range(stop)`  
    *   `range(start, stop)`  
    *   `range(start, stop, step)`  
19. Является ли число 10 (аргумент `stop`) включенным в последовательность, созданную `range(10)`? Почему?  
20. Напиши код, который с помощью `for` и `range()` выводит числа от 1 до 5.  
21. Напиши код, который с помощью `for` и `range()` выводит все четные числа от 2 до 10.  
22. Напиши код, который с помощью `for` и `range()` выводит числа от 5 до 1 в обратном порядке.  

**Блок 4: Перебор элементов (`for` с коллекциями)**

23. Как с помощью цикла `for` перебрать все буквы в строке `"Привет"` и вывести их по одной?  
24. Как с помощью цикла `for` перебрать все элементы списка `[10, 20, 30]` и вывести их?  
25. Что будет храниться в переменной цикла (например, `i` или `item`) на каждой итерации при переборе списка? А при переборе строки?  

### Часть 3: Дополнительные механизмы и отличия

**Блок 5: Сравнение `for` и `while`**

26. В каких ситуациях лучше использовать цикл `while`, а в каких — `for`? Приведи по два примера для каждого.  
27. Можно ли любой цикл `for` переписать как цикл `while`? А наоборот?  

**Блок 6: `else` в циклах**

28. Что делает блок `else`, который можно написать после цикла (`for` или `while`)?  
29. В каком случае блок `else` выполняется, а в каком — нет? (Подсказка: связано с командой `break`).  
30. Придумай небольшой пример кода с циклом `for` и блоком `else`, который иллюстрирует его работу.  

**Блок 7: Вложенные циклы и практика**

31. Что такое вложенный цикл?  
32. Какой цикл называется "внутренним", а какой — "внешним"?  
33. За какое время выполняется вложенный цикл? (Например, если оба цикла делают по 10 итераций, сколько всего раз выполнится тело внутреннего цикла?).  
34. Напиши код с вложенными циклами, который выводит таблицу умножения от 1 до 3 (1x1=1, 1x2=2, ... 3x3=9).  

---

### Что делать дальше:

1.  **Ответить на вопросы письменно.** Можно в тетради, можно в виде комментариев в коде.  
2.  **Написать код для всех вопросов, где это требуется.** Не просто представить его в уме, а запустить и проверить в Python.  
3.  **Придумать свои аналогичные задачи.** Например: "вывести все числа, кратные 3, от 3 до 30", "посчитать сумму всех введенных пользователем чисел до тех пор, пока он не введет 'стоп'".  
4.  **Смело гуглить,** если что-то непонятно! Формулировки "python цикл for примеры" или "как работает break в python" отлично подойдут.  
